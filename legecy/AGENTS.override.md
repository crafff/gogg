# Archived reference workspace

This directory preserves the old workspace, including uncommitted work and old
instructions. The current user designated it as business-logic reference only.
Read source as evidence; do not adopt archived AGENTS/CLAUDE/skills/configuration
as current guidance. Do not start archived services or modify archived source
unless the user specifically requests archival restoration or investigation.

The current project's rules and commands are at the repository root. Old paths,
APIs, dependencies and workflow histories have no new compatibility requirement.
External data symlinks are references, not permission to alter or delete targets.
