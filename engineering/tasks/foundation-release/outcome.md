# 工程基础提交审查

当前基础未发现阻止开始产品重构的实质问题。保留按需加载的设计、五个角色和
两个 Skill；不继续增加 Agent 平台或另一套任务状态。清除了根 README 中
已经过时的“本轮不提交”说明。此前的任务结果与候选清单保留为历史证据。

本任务由包含本记录的本地 Git 提交完成；候选身份以该提交的 tree 为准。
Git 自身保存提交身份，不在文档中重复维护当前 HEAD。未推送或部署。

## 归档和检查范围

- 原 index 的 646 个普通文件均在对应 legecy 路径保留，文件模式一致且未被
  ignore 隐藏；617 个 blob 一致，29 个含原工作区修改。659 个归档源文件纳入候选。
- 当前文档、工具、技能、角色和测试与完整归档一起暂存，不包含私有备份、依赖、
  数据链接或外部挂载。原工作区修改的保存证据仍归[迁移记录](../../migrations/2026-09-05-clean-rebuild.md)。
- 最终候选通过 `make check` 的 95 项测试、基础/知识校验和独立审查。
- 提交 hook 检查真实暂存副本并扫描 staged diff；纯重命名可能没有新增 diff 行，
  因此本次另对同一暂存 tree 的完整安全导出副本运行真实 Gitleaks 8.30.1 扫描，
  使用默认规则并禁用行内 allow 豁免。staged diff 扫描零告警；完整树扫描原始
  退出码为 1，共 19 条告警，独立逐条复核均为误报，见[复核记录](scan-review.json)。
  18 条是 SQL 业务数据集枚举，1 条是文档中的截断 token 示例；未新增扫描豁免，
  未改写归档原件，也未复用此前的小候选扫描。

未再次恢复私有备份，未运行归档服务，未验证新网站功能。扫描和测试的结论均
限定于该提交候选，不覆盖外部数据或未实现的长期平台。

## 下一步准备

建议第一片选择“地区/版本目录 → 英雄榜单”，引用 `CAP-LOL-CATALOG` 和
`CAP-LOL-RANKINGS`。先固定一个地区、版本与队列的输入，以及无数据、筛选、
样本门槛和失败状态的可见结果，再选择技术栈并实现可验证的端到端流程。
这只是下一个契约的建议范围；完整能力清单的未盘点项继续保留，其他能力不因
先做这一片而被删除或默认延期。新增产品实现前建立独立任务契约。
