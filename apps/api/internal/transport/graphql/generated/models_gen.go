// Code generated by github.com/99designs/gqlgen, DO NOT EDIT.

package gqlgenerated

import (
	"bytes"
	"fmt"
	"io"
	"strconv"
)

// Per-champion ranking row. Mirrors the legacy REST shape so the new
// GraphQL surface and /api/v1/rankings/champions stay byte-comparable
// for a given filter, per ADR-0003.
type ChampionRanking struct {
	ChampionID   int    `json:"championId"`
	ChampionName string `json:"championName"`
	// Positions the champion played for at least positionThreshold % of
	//   their games. For per-position queries this is a single-element list.
	TeamPosition []string `json:"teamPosition"`
	Games        int      `json:"games"`
	Wins         int      `json:"wins"`
	Losses       int      `json:"losses"`
	WinRate      float64  `json:"winRate"`
	PickRate     float64  `json:"pickRate"`
	BanRate      float64  `json:"banRate"`
	Kda          float64  `json:"kda"`
}

// Filter for championRankings. Every field is optional with sensible
// defaults; clients pass only what differs from the defaults. Mirrors
// the legacy REST query params 1:1.
type ChampionRankingsFilter struct {
	// Queue ID. 420 = Ranked Solo/Duo, 440 = Ranked Flex.
	QueueID *int `json:"queueId,omitempty"`
	// Exact game version (e.g. "14.23.1") or "latest". Empty = all.
	Version *string `json:"version,omitempty"`
	// Region code (KR, NA1). Empty = all.
	Region *string `json:"region,omitempty"`
	// Rank bucket. ALL = no tier filter.
	TierGroup *TierGroup `json:"tierGroup,omitempty"`
	// Drop champions with fewer games than this. 0 = no floor.
	MinGames *int `json:"minGames,omitempty"`
	// Cap the result rows. -1 = unlimited.
	Limit *int `json:"limit,omitempty"`
	// Minimum share of games per position to list the position in
	//   teamPosition. 0.5 = position is "primary" if played in ≥50% of
	//   games. Ignored when `position` is set.
	PositionThreshold *float64 `json:"positionThreshold,omitempty"`
	// Restrict to a single team position (TOP, JUNGLE, MIDDLE, BOTTOM,
	//   UTILITY). When set the resolver calls the per-position path and
	//   every result row's teamPosition is exactly [position].
	Position *string `json:"position,omitempty"`
}

// Top-level query root. Domain schemas extend this with their own fields
// so each module's surface stays in its own .graphql file.
type Query struct {
}

// Bundle returned by the championRankings query. totalMatches is the
// cross-row denominator used to compute pick/ban rates; clients display
// it as the dataset size badge.
type RankingsResult struct {
	Items        []*ChampionRanking `json:"items"`
	TotalMatches int                `json:"totalMatches"`
	// When the caller passed version="latest", this echoes the version
	//   that was resolved (e.g. "14.23.1"). Null otherwise.
	ResolvedVersion *string `json:"resolvedVersion,omitempty"`
}

// Coarse rank bucket used to filter rankings. Matches the legacy REST
// tier_group query param 1:1 so /api/v1 and /graphql return the same
// aggregations for any given filter combination.
type TierGroup string

const (
	// All tiers (no filter).
	TierGroupAll TierGroup = "ALL"
	// MASTER only.
	TierGroupMaster TierGroup = "MASTER"
	// MASTER + GRANDMASTER + CHALLENGER.
	TierGroupMasterPlus TierGroup = "MASTER_PLUS"
	// GRANDMASTER only.
	TierGroupGrandmaster TierGroup = "GRANDMASTER"
	// GRANDMASTER + CHALLENGER.
	TierGroupGrandmasterPlus TierGroup = "GRANDMASTER_PLUS"
	// CHALLENGER only.
	TierGroupChallenger TierGroup = "CHALLENGER"
)

var AllTierGroup = []TierGroup{
	TierGroupAll,
	TierGroupMaster,
	TierGroupMasterPlus,
	TierGroupGrandmaster,
	TierGroupGrandmasterPlus,
	TierGroupChallenger,
}

func (e TierGroup) IsValid() bool {
	switch e {
	case TierGroupAll, TierGroupMaster, TierGroupMasterPlus, TierGroupGrandmaster, TierGroupGrandmasterPlus, TierGroupChallenger:
		return true
	}
	return false
}

func (e TierGroup) String() string {
	return string(e)
}

func (e *TierGroup) UnmarshalGQL(v any) error {
	str, ok := v.(string)
	if !ok {
		return fmt.Errorf("enums must be strings")
	}

	*e = TierGroup(str)
	if !e.IsValid() {
		return fmt.Errorf("%s is not a valid TierGroup", str)
	}
	return nil
}

func (e TierGroup) MarshalGQL(w io.Writer) {
	fmt.Fprint(w, strconv.Quote(e.String()))
}

func (e *TierGroup) UnmarshalJSON(b []byte) error {
	s, err := strconv.Unquote(string(b))
	if err != nil {
		return err
	}
	return e.UnmarshalGQL(s)
}

func (e TierGroup) MarshalJSON() ([]byte, error) {
	var buf bytes.Buffer
	e.MarshalGQL(&buf)
	return buf.Bytes(), nil
}
