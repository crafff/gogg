go 1.26.1

toolchain go1.26.4

// The workspace currently contains the root module, which owns the
// apps/ and packages/ trees. Keep the `go` directive >= the module's
// go.mod directive. `toolchain` pins the actual compiler so local dev
// and CI use the same version.

use (
	.
)
